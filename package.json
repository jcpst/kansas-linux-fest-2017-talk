{
  "scripts": {
    "build": "harp compile src build",
    "clean": "rimraf build",
    "deploy": "harp compile src /tmp/__temp_static_content",
    "start": "harp server src"
  },
  "devDependencies": {
    "harp": "^0.23.0",
    "rimraf": "^2.6.1"
  }
}
